import React from "react"
import { Helmet } from 'react-helmet';

const SEO = () => {
    return(
        <Helmet encodeSpecialCharacters={false} title={'Testing & Testing'}></Helmet>
    )
};

export default SEO;